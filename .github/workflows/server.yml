name: Server CI

on:
  push:
    branches:
      - rtype_server-dev
  pull_request:
    branches:
      - rtype_server-dev

jobs:
  unit_tests:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
      - name: Run Linter
        run: ./lint.sh
